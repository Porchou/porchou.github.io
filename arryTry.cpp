#include<stdio.h>


	//1 wxample= 123456=6number (count)
	//2 write 1234=>4321
	//3 calculate input number and then make it have 1 svaykun it
	//4 logis and small
main()
{
/*
	// 1. Program to find the largest number in an array of 5 numbers
	int arr[5], i , largest=0;

	for (i=0;i<5;i++)
	{
		printf("Enter value of Array : ");
		scanf(" %d", &arr[i]);
		if (arr[i]>largest)
		{
			largest=arr[i];
			
		}

	}
	printf("The largest Number is : %d", largest);
*/

/*
	// 2. Program to find the smallest number in an array of 5 numbers	
	int i, smallest,arr[5];
	for ( i=0;i<5;i++ )
	{
		printf("Enter value of Array : " );
		scanf("%d",&arr[i]);
		smallest=arr[0];
		if ( arr[i]<smallest)
		{
			smallest=arr[i];
		}
	}
	printf("The smallest number is : %d",smallest);
*/	
/*	
	// 3. Program to print the fibonacci series upto a range may be till 50
	int a[50],b[50],i=0,number=0;
	printf("Enter the start point : ");
//	for (i=0;i<50;i++)
//	{
//	
//		scanf("%d", &b);
//		a=b;
//		printf("%d %d\n",a,b);
//		
//	}
	scanf("%d", &number);
	while(i<50)
	{
		
	//	a[i]=b[i];
	//	printf("%d %d\n",a[i],b[i]);
		a[i]=b[i]=number;
		a[i]=b[i]+number;
		printf("%d %d\n",a[i],b[i]);
		
		i++;
		
	}
	
	 
		
*/
/*	
	// 4. Program to find the number of digits in a number.
	string number; //how to use string in this program to len many charactor;
	printf("Enter number : ");
	scanf("%c", &number);
	printf("%c",number);
*/
/*
//5. Program to reverse a number

	int i,number[8],arrnum[8];
	printf("Enter number to Reverse : ");
	for (i=0;i<8;i++)
	{
		scanf("%d", &number[i]);
	}
	printf("%d",number[i]);
*/

	
		
	
	
	
	
	
}
